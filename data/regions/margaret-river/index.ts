import { RegionData } from '../../../types';
import { MARGARET_RIVER } from './region';
import { MARGARET_RIVER_WINERIES } from './wineries';
import { MARGARET_RIVER_WINES } from './wines';
import { MARGARET_RIVER_EXPERIENCES } from './experiences';
import { MARGARET_RIVER_EVENTS } from './events';

export const MARGARET_RIVER_DATA: RegionData = {
  region: MARGARET_RIVER,
  wineries: MARGARET_RIVER_WINERIES,
  wines: MARGARET_RIVER_WINES,
  experiences: MARGARET_RIVER_EXPERIENCES,
  events: MARGARET_RIVER_EVENTS,
};
