// DEPRECATED shim — legacy numeric-id data for components not yet converted
// to useCatalog(). Will be deleted once the rework lands everywhere.
export { WINERIES, WINES } from './legacy-wineries';
