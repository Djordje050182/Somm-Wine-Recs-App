import React from 'react';
import WineLibrary from '../../components/WineLibrary';

// The shoppable wine library for the active region.
const WinesPage: React.FC = () => <WineLibrary />;

export default WinesPage;
