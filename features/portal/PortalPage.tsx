import React from 'react';
import PartnerPortal from '../../components/PartnerPortal';

// The winery-side portal — being rebuilt as features/portal/* with listing
// editor, wine manager, offers, membership billing and analytics.
const PortalPage: React.FC = () => <PartnerPortal />;

export default PortalPage;
